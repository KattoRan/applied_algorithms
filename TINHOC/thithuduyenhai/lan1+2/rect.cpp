#include<bits/stdc++.h>
using namespace std;
const int maxm=1e3+7;
const int maxn=1e3+7;
int m,n,a[maxm][maxn];
int b[maxm][maxn];
int main()
{
     ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin>>m>>n;
    for(int i=1;i<=m;i++)
        for(int i=1;i<=n;i++) cin>>a[i][j];
    for(int i=1;i<=m;i++)
        for(int i=1;i<=n;i++)


}
